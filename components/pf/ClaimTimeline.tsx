import {ClaimStatus} from '@/lib/deterministic/status'; const steps:[ClaimStatus,string][]=[['submitted','Claim submitted'],['field_office','Documents checked'],['under_review','Being reviewed'],['approved','Decision'],['settled','Settlement']]; export function ClaimTimeline({status}:{status:ClaimStatus}){const i=steps.findIndex(x=>x[0]===status);return <ol className="timeline">{steps.map(([id,label],n)=><li key={id} className={n<i?'done':n===i?'current':''}><span>{n<i?'✓':n===i?'●':'○'}</span><div><b>{label}</b><small>{n===i?'You are here in this demo journey.':''}</small></div></li>)}</ol>}
