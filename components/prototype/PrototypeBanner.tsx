'use client'; import {copy} from '@/lib/i18n'; import {useLanguage} from '@/components/layout/Shell'; export function PrototypeBanner(){const {language}=useLanguage();return <aside className="prototype" role="note">✦ {copy[language].prototype}</aside>}; export function SyntheticLabel(){const {language}=useLanguage(); return <span className="synthetic">{copy[language].synthetic}</span>}
