'use client'; import Link from 'next/link'; import {claim} from '@/data/mock'; import {statusInfo} from '@/lib/deterministic/status'; import {ClaimTimeline} from '@/components/pf/ClaimTimeline'; import {SyntheticLabel} from '@/components/prototype/PrototypeBanner'; import {Button} from '@/components/ui/Button'; export default function Status(){const s=statusInfo[claim.status];return <div className="page"><div className="crumb">Home / PF status</div><SyntheticLabel/><h1>{s.title}</h1><p className="lead">Claim {claim.id} · a guided explanation, not just a label.</p><div className="two"><section className="card status-card"><p className="eyebrow">Current status</p><h2>{s.title}</h2><dl><div><dt>What this means</dt><dd>{s.means}</dd></div><div><dt>What you need to do</dt><dd>{s.action}</dd></div><div><dt>What happens next</dt><dd>{s.next}</dd></div><div><dt>Estimated timeframe</dt><dd>{claim.estimate}<small>Illustrative estimate based on synthetic historical data.</small></dd></div></dl></section><section className="card"><p className="eyebrow">Your journey</p><ClaimTimeline status={claim.status}/></section></div><div className="next-actions"><Link href="/mismatch"><Button>Check for a mismatch</Button></Link><Link href="/passbook"><Button className="secondary">See passbook explained</Button></Link></div></div>}
