import Link from 'next/link'; import {passbook} from '@/data/mock'; import {withdrawablePF} from '@/lib/deterministic/passbook'; import {SyntheticLabel} from '@/components/prototype/PrototypeBanner'; import {Button} from '@/components/ui/Button'; const money=(n:number)=>`₹${n.toLocaleString('en-IN')}`; export default function Passbook(){return <div className="page"><div className="crumb">Home / Passbook</div><SyntheticLabel/><h1>Your passbook, made clearer</h1><p className="lead">The PF you can understand—and pension kept separate.</p><div className="two"><section className="card balance"><p className="eyebrow">Your PF</p><div><span>Employee share</span><b>{money(passbook.employeeShare)}</b></div><div><span>Employer share</span><b>{money(passbook.employerShare)}</b></div><div><span>Interest</span><b>{money(passbook.interest)}</b></div><hr/><div className="total"><span>Withdrawable PF</span><b>{money(withdrawablePF(passbook))}</b></div></section><section className="card pension"><p className="eyebrow">Your pension</p><div><span>EPS</span><b>{money(passbook.eps)}</b></div><p>EPS is your pension component. It is not the same as the PF balance shown above.</p><small>All figures are synthetic and illustrative.</small></section></div><Link href="/grievance"><Button>Need a follow-up? Check grievance</Button></Link></div>}
