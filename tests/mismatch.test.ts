import {describe,it,expect} from 'vitest';import{compareIdentityRecords}from'../lib/deterministic/identity'; const c=(a:string,b=a,d=a)=>compareIdentityRecords({name:a},{name:b},{name:d});describe('identity matcher',()=>{it('normalizes exact, case, space, punctuation',()=>{expect(c(' Mohd. Aslam ','mohd aslam','MOHD-ASLAM').level).toBe('match')});it('flags small typo',()=>expect(c('Asha Devi','Asha Devi','Asha Devii').level).toBe('minor_mismatch'));it('flags abbreviation variants as ambiguous',()=>expect(c('Mohd Aslam','Mohammad Aslam','Mohd Aslam').level).toBe('ambiguous'));it('flags major mismatch',()=>expect(c('Asha','Ramesh','Asha').level).toBe('major_mismatch'))})
