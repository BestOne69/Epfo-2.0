import{describe,it,expect}from'vitest';import{canTransition}from'../lib/deterministic/status';describe('status transitions',()=>{it('allows declared paths',()=>{expect(canTransition('submitted','under_review')).toBe(true);expect(canTransition('under_review','approved')).toBe(true);expect(canTransition('approved','settled')).toBe(true)});it('rejects invalid transitions',()=>expect(canTransition('submitted','settled')).toBe(false)})
