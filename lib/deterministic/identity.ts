export type IdentityRecord={name:string; dob?:string; bankName?:string}; export type MismatchLevel='match'|'minor_mismatch'|'ambiguous'|'major_mismatch';
export const normalize=(value:string)=>value.toLowerCase().trim().replace(/[.\-,_/]/g,' ').replace(/\s+/g,' ');
const distance=(a:string,b:string)=>{const p=Array.from({length:b.length+1},(_,i)=>i);for(let i=1;i<=a.length;i++){let prev=p[0];p[0]=i;for(let j=1;j<=b.length;j++){const old=p[j];p[j]=Math.min(p[j]+1,p[j-1]+1,prev+(a[i-1]===b[j-1]?0:1));prev=old}}return p[b.length]};
export function compareIdentityRecords(aadhaar:IdentityRecord,uan:IdentityRecord,bank:IdentityRecord){const names=[aadhaar,uan,bank].map(x=>normalize(x.name));const max=Math.max(...names.map(n=>n.length));const score=1-Math.max(distance(names[0],names[1]),distance(names[0],names[2]))/Math.max(max,1); const hasAbbreviation=names.some(n=>/\bmohd\b/.test(n))&&names.some(n=>/\bmohammad\b/.test(n)); let level:MismatchLevel=score===1?'match':hasAbbreviation?'ambiguous':score>.88?'minor_mismatch':score>.7?'ambiguous':'major_mismatch';return {level,score,names,needsAi:level==='ambiguous'} }
